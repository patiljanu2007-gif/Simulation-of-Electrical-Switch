\\Online C compiler to run C program online
#include <stdio.h>
int main() {
    int amount;

    printf("Enter amount: ");
    scanf("%d", &amount);

    if (amount < 2000) {
        printf("payment not accepted\n");
    } else {
        printf("payment accepted\n");
    }

    return 0;
}
